rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow admin (any authenticated user in this prototype) to read and write all orders
    match /orders/{orderId} {
       allow read, write: if request.auth != null;
    }
  }
}
